-Crear la interfaz grafica con layouts para que quede mejor organizada y se redimensionen los objetos al maximizar o minimizar
-Primeo hacer que la agenda funcione bien, usando solo la RAM, y una vez que funcione adaptar las clases a SQL
